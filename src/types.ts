
type HierarchyNode = {
    id: string,
    data: string,
    children: HierarchyNode[],
    type?: string,
    x?: number,
    y?: number,
    width?: number,
    height?: number,
    familyWidth?: number,
}